@AGENTS.md

LeadShoot-specific quick refs for Claude Code: the project MCP server is
registered via ./.mcp.json (`leadshoot mcp`); the usage skill lives at
skills/leadshoot/SKILL.md; run tests with `.venv/bin/python -m pytest tests/ -q`.
